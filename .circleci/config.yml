version: 2.1

jobs:
  build:
    docker:
      - image: cimg/node:16.17.0
    steps:
      - checkout
      - run:
          background: true
          command: node server.js
      - run: sleep 20s && curl http://127.0.0.1:8000/

workflows:
  workflows:
    jobs:
      - build